# ============= ENVIRONMENT CONFIGURATION =============

# Flask Environment (development, production, testing)
FLASK_ENV=development

# Secret key for session management (REQUIRED in production)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here

# ============= DATABASE CONFIGURATION =============

# VERCEL POSTGRESQL (Recommended)
# For both development and production environments
# Development: Use your Vercel development database URL
# Production: Vercel automatically injects this via connected PostgreSQL database
DATABASE_URL=postgresql://username:password@host:port/database

# ============= VERCEL POSTGRES SETUP =============
# 1. Create databases in Vercel Dashboard > Storage > PostgreSQL
# 2. Development: Copy DATABASE_URL to .env.local
# 3. Production: Connect database to your Vercel project (auto-injects DATABASE_URL)
# 4. Both environments will use the same application code

# ============= OPTIONAL SETTINGS =============

# Debug mode (default: false in production)
DEBUG=false

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Upload directory (default: uploads for local, /tmp/uploads for Vercel)
UPLOAD_FOLDER=uploads

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:8000
# 5. Add DATABASE_URL to Vercel environment variables
# 6. Redeploy - your data will persist forever!

# Upload folder (default: uploads)
UPLOAD_FOLDER=uploads

# Maximum file upload size in bytes (default: 16MB)
MAX_CONTENT_LENGTH=16777216

# CORS allowed origins (comma-separated)
# For development: http://localhost:5000,http://127.0.0.1:5000
# For production: https://yourdomain.com
CORS_ORIGINS=*

# For Vercel deployment (set automatically by Vercel)
# VERCEL=1
