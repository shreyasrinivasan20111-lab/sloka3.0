# ============= ENVIRONMENT CONFIGURATION =============

# Flask Environment (development, production, testing)
FLASK_ENV=development

# Secret key for session management (REQUIRED in production)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here

# ============= DATABASE CONFIGURATION =============

# LOCAL DEVELOPMENT (DuckDB) - just set the path
DB_PATH=student_courses.db

# PRODUCTION (PostgreSQL) - use either DATABASE_URL OR individual components
# Option 1: Full connection string (recommended for Supabase/Neon/PlanetScale)
# DATABASE_URL=postgresql://username:password@host:port/database

# Option 2: Individual components
# DB_HOST=your-db-host.supabase.co
# DB_PORT=5432
# DB_NAME=postgres
# DB_USER=your-username  
# DB_PASSWORD=your-password

# ============= SUPABASE QUICK SETUP =============
# 1. Go to https://supabase.com (free tier available)
# 2. Create new project
# 3. Go to Settings > Database
# 4. Copy connection string to DATABASE_URL above
# 5. Add DATABASE_URL to Vercel environment variables
# 6. Redeploy - your data will persist forever!

# Upload folder (default: uploads)
UPLOAD_FOLDER=uploads

# Maximum file upload size in bytes (default: 16MB)
MAX_CONTENT_LENGTH=16777216

# CORS allowed origins (comma-separated)
# For development: http://localhost:5000,http://127.0.0.1:5000
# For production: https://yourdomain.com
CORS_ORIGINS=*

# For Vercel deployment (set automatically by Vercel)
# VERCEL=1
