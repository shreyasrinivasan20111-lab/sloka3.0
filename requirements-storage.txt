# Optional dependencies for external DuckDB storage
# Install only if you're using the respective cloud provider

# For AWS S3 storage
boto3>=1.34.0

# For Google Cloud Storage  
google-cloud-storage>=2.10.0

# For HTTP requests (usually already available)
requests>=2.31.0
